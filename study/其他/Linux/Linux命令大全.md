```
---------Linux 命令 ------

cp file /var/file —— 将当前目录下的file复制到 /var/ 文件夹下 并命名为 file

sudo su root —— 变更为 root 用户

---------Ubuntu 命令 ------

apt-get upgrade —— 更新任何已安装的软件包

apt-get dist-upgrade —— 将系统升级到新版本


```